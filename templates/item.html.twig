{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <h4>{{ article.title }}</h4>
        <p>{{ article.text }}</p>
        <p>Created: {{ article.createdAt|date }}</p>
        <p>Updated at: {{ article.updatedAt|date }}</p>
    </div>
{% endblock %}